# Database Schema Reference

A human-readable snapshot of the current Supabase/Postgres schema, grouped by
feature instead of by migration date. **This file is generated by reading
`supabase/migrations/` — it is not itself a source of truth and is not run
against the database.** If it ever disagrees with the migrations, the
migrations win; re-generate this file rather than trusting it blindly.

Every table has Row-Level Security (RLS) enabled and no table is reachable via
the Data API until explicitly `grant`ed — Supabase's "auto-expose new tables"
setting is off project-wide, so every table below has its access spelled out
in its own migration on purpose, not by default.

## How to read this

- **Columns** — the current shape, not the history of `alter table` calls that built it.
- **RLS** — who can select/insert/update/delete, in plain English.
- **Functions** section at the bottom — every `security definer` RPC in one place, since that's the actual business logic layer (see below for why they live in migrations at all).

---

## Auth & Profiles

### `profiles`
One row per `auth.users` row — the signup fields Supabase Auth doesn't store itself.

| Column | Type | Notes |
|---|---|---|
| `id` | uuid PK | = `auth.users.id`, cascades on user delete |
| `first_name`, `last_name`, `phone` | text | required |
| `role` | text | `customer` \| `employee` \| `owner` |
| `created_at` | timestamptz | |

**RLS:** a user can select/update only their own row. There's no insert policy at all — rows are created only by the `handle_new_user()` trigger (see Functions), never directly by a client. An additional narrow policy (added when staff management shipped) lets an owner select the profile of anyone with a `restaurant_staff` row at one of their restaurants, via the `is_restaurant_owner_of_employee()` function — added as a function specifically to avoid an RLS recursion bug (see the callout at the bottom).

---

## Restaurants & Hours

### `restaurants`
| Column | Type | Notes |
|---|---|---|
| `id` | uuid PK | |
| `owner_id` | uuid FK → `auth.users` | not unique — one owner can have several restaurants |
| `name` | text | |
| `capacity` | integer, nullable | plain manual cap; `null` = unlimited. **Only meaningful/written when the restaurant has no sections and no active layout** — once either exists, effective capacity is computed live from them instead (app logic, see `src/lib/capacity-cascade.ts`) and this column is left stale on purpose. |
| `default_stay_minutes` | integer, not null, default 90 | constrained to 30–180, matching `create_reservation()`'s own duration cap |
| `created_at` | timestamptz | |

**RLS:** select is open to any authenticated user (any role — needed so customers/employees can browse). Insert is restricted to accounts with `profiles.role = 'owner'`. Update/delete restricted to the owning `owner_id`.

### `restaurant_hours`
One row per open interval (not per day) — a day can have multiple rows (split shifts), and an interval can run across midnight as two adjacent rows on neighboring days.

| Column | Type | Notes |
|---|---|---|
| `restaurant_id` | uuid FK → `restaurants`, cascade | |
| `day_of_week` | smallint 0–6 | Postgres `extract(dow ...)` convention: 0 = Sunday |
| `start_minute`, `end_minute` | smallint 0–1440 | minutes since midnight; `end > start`; 1440 = runs to exact midnight |

**RLS:** select open to all authenticated users; insert/update/delete restricted to the restaurant's owner.

> This table went through two schema revisions (`open_time`/`close_time` + `is_24h`/`closes_next_day` flags → plain `start_minute`/`end_minute` intervals) before landing here — a good example of "the migrations are history, this doc is the destination."

---

## Staff / Employee Applications

### `restaurant_staff`
One row per (restaurant, employee) pair.

| Column | Type | Notes |
|---|---|---|
| `restaurant_id` | uuid FK → `restaurants`, cascade | |
| `employee_id` | uuid FK → `auth.users`, cascade | |
| `status` | text | `pending` \| `accepted` — **no `rejected` status**: a reject or a cancel just deletes the pending row |
| unique | `(restaurant_id, employee_id)` | |

**RLS:** an employee sees their own applications; an owner sees applications to their own restaurants. Insert restricted to `profiles.role = 'employee'`, always starting `pending`. The only update path is an owner flipping `pending → accepted` for their own restaurant. Delete covers both an employee cancelling their own pending row and an owner rejecting/removing any row at their restaurant.

---

## Sections & Table Layout

Two independent, optional features — a restaurant can have sections, a table layout, both, or neither (see `ISSUES.md`'s "Decided" notes for the full reasoning).

### `sections`
| Column | Type | Notes |
|---|---|---|
| `restaurant_id` | uuid FK → `restaurants`, cascade | |
| `name` | text | unique per restaurant |
| `capacity` | integer, `>= 0` | manually typed **only** when no active layout exists; once one does, effective capacity is the live sum of that section's tables' seats instead |
| `color_index` | smallint 0–7 | index into the fixed 8-color palette in `src/lib/section-colors.ts` — only the index is stored, never a literal color, so hues can be retuned without a migration |

**RLS:** select open to all; insert/update/delete restricted to the restaurant's owner.

### `layouts`
| Column | Type | Notes |
|---|---|---|
| `restaurant_id` | uuid FK → `restaurants`, cascade | |
| `name` | text | unique per restaurant |
| `is_active` | boolean | **multiple layouts can be active at once** (e.g. one per floor) — capacity is derived from the union of every active layout's tables |

**RLS:** same owner-write / any-authenticated-read pattern as `sections`.

### `tables`
| Column | Type | Notes |
|---|---|---|
| `restaurant_id` | uuid FK → `restaurants`, cascade | |
| `layout_id` | uuid FK → `layouts`, cascade | |
| `section_id` | uuid FK → `sections`, **set null** on delete | nullable even once the restaurant has sections — "every table needs a section once any exist" is an app-level rule, never a DB constraint |
| `name` | text | unique per **layout**, not per restaurant (two tables in different layouts, or two tables restaurant-wide, may share a name) |
| `seats`, `x`, `y`, `width`, `height` | integer, all positive | grid units for the floor-plan canvas; every table is an axis-aligned rectangle, no rotation |

**RLS:** select open to all. Insert/update additionally verify — not just that `restaurant_id` is owned by the caller, but that `layout_id` and `section_id` actually belong to *that same* restaurant. This was a real fix (see the callout below): without it, an owner could attach their own table to another owner's layout/section, since layout/section ids aren't secret.

---

## Reservations

### `reservations`
| Column | Type | Notes |
|---|---|---|
| `restaurant_id`, `customer_id` | uuid FK, cascade | |
| `party_size` | integer, `> 0` | |
| `starts_at`, `ends_at` | timestamptz | `ends_at > starts_at`, capped at 24h |
| `status` | text | `confirmed` \| `preparing_order` \| `order_prepared` \| `ongoing` \| `completed` \| `no_show` \| `cancelled` (`cancelled` is in the check constraint but unreachable through any RPC today — see the status-lifecycle callout below) |

**RLS:** only `select` is granted to `authenticated` — **there is no insert/update/delete grant at all.** The only way a reservation is ever created is `create_reservation()`, and the only way its status ever changes afterward is `update_reservation_status()` (see Functions), both `security definer` so they can write despite the missing grant. This is deliberate: it's the one place hours/capacity/conflict/transition validation happens, and skipping it isn't possible from a raw client call. Select policies cover the customer who owns it, the restaurant's owner, and its accepted staff.

> **Status lifecycle:** `confirmed -> [preparing_order -> order_prepared ->] ongoing -> completed`, plus `confirmed`/`order_prepared -> no_show` (only once `starts_at` has passed) — driven by accepted staff (not the owner) from the employee reservations page (`/employee/restaurants/[id]`), via `update_reservation_status()` (`supabase/migrations/20260917140000_reservation_status_lifecycle.sql`). `preparing_order`/`order_prepared` only apply when the reservation has a linked confirmed order; otherwise the flow skips straight `confirmed -> ongoing`. `cancelled` stays unreachable through this RPC — a customer-initiated cancel is still its own separate backlog item. "Active" (occupying a table/section, for every capacity/availability check and the delete/capacity-decrease guards below) used to mean exactly `status = 'confirmed'` everywhere; it's now `is_active_reservation_status(status)` (`confirmed`/`preparing_order`/`order_prepared`/`ongoing`), so a reservation mid-service still blocks new bookings the same way a plain `confirmed` one always has. A reservation that runs out its time is finished automatically by `finish_expired_reservations()` (`20260918120000_auto_finish_reservations.sql`), run every minute by `pg_cron`: `ongoing` becomes `completed`, and any of `confirmed`/`preparing_order`/`order_prepared` becomes `no_show` (the guest never showed, even if the kitchen had started their order). No `ends_at` adjustment is needed there — capacity and the exclusion constraint already treat an ended slot as free via the same `... and ends_at >= now()` pattern; only *early* completion/no-show has to shrink it (below). Completing (or no-show-ing) a reservation *before* its booked `ends_at` additionally shrinks `ends_at` down to (approximately) `now()` on both the `reservations` row and its `reservation_tables` rows — the only way to free the exclusion constraint below for the remainder of the original slot, since that constraint has no idea what `status` even means. Symmetrically, marking a reservation `ongoing` *before* its booked `starts_at` moves `starts_at` up to `now()` on the same two places (`20260918100000_ongoing_moves_start_to_now.sql`), so an ongoing reservation has always genuinely started and an early completion's `ends_at` can never land before its `starts_at`. That earlier start is rejected with a readable message if it would collide with another reservation already holding one of its tables (the exclusion constraint), or if it is booked to start more than 60 minutes from now — early start is only allowed within 60 minutes of the booked time (`20260918130000_reservation_lifecycle_review_fixes.sql`), which also keeps it well inside the `starts_at + 24h` cap on `ends_at`. Once a reservation's `ends_at` has passed it accepts no transitions at all (the cron sweep is about to finish it). Execute on `update_reservation_status` is granted to `authenticated` only; `restaurant_peak_reserved_capacity`/`section_peak_reserved_capacity` are revoked from every app role (reached only through triggers and other `security definer` functions), and a partial index on `reservations(ends_at)` for the active statuses backs the cron sweep.

### `reservation_tables`
One row per table a reservation occupies (several for a multi-table booking).

| Column | Type | Notes |
|---|---|---|
| `reservation_id`, `table_id` | uuid FK, cascade | |
| `starts_at`, `ends_at` | timestamptz | denormalized copy of the parent reservation's range — required because the exclusion constraint below needs both columns on the same row |
| — | **exclusion constraint** | `exclude using gist (table_id with =, tstzrange(starts_at, ends_at) with &&)` — this is the actual double-booking prevention, enforced by Postgres itself, not application code |

**RLS:** select-only, scoped to the reservation's customer/owner/accepted-staff, same shape as `reservations`.

### `reservation_sections`
Used only when a restaurant has sections but no active layout (once a layout is active, tables carry the section instead).

| Column | Type | Notes |
|---|---|---|
| `reservation_id`, `section_id` | uuid FK, cascade | |
| `party_size` | integer, `> 0` | this reservation's share of that section — several rows per reservation when a party spills across sections |

**RLS:** same select-only shape as the two tables above.

> **Deletion/capacity guards:** `reservation_tables.table_id` and `reservation_sections.section_id` are both `on delete cascade` from `tables`/`sections` — deleting either would otherwise silently strip an active, not-yet-ended reservation's seating with no trace. Four triggers close this (see Functions below): deleting a `tables`/`sections` row is rejected outright while an active (`is_active_reservation_status(status)`) reservation with `ends_at >= now()` still references it, and shrinking `restaurants.capacity`/`sections.capacity` is rejected below the peak already-committed load (summed across overlapping active, not-yet-ended reservations, not just checked one at a time). "Active"/"peak load" both use the same time-based rule as the customer reservations page (`is_active_reservation_status(status) and ends_at >= now()`). The delete-blocking triggers' raised messages name the specific table/section and its earliest active reservation's date/time and party size, rather than a generic "can't delete" string. Three more functions — `tables_with_active_reservations`, `sections_with_active_reservations`, `sections_peak_reserved_capacity` — let the owner's edit form (`edit-restaurant-form.tsx`) check every candidate id *before* attempting any delete or capacity decrease, so one save reports every blocker at once instead of failing on the first one a trigger happens to hit. `tables_with_active_reservations` also reports each blocked table's layout name, since table names are only unique per layout (not per restaurant) — the form groups its message by layout once more than one is involved, to avoid an ambiguous flat list. All three join up to `restaurants` and require `owner_id = auth.uid()` (`supabase/migrations/20260917130000_scope_capacity_functions_to_owner.sql`) — an earlier version had no ownership check at all, which since `tables`/`sections` are public-read let any authenticated user learn a stranger's reservation time/party size for an arbitrary table/section id; caught in code review, see `ISSUES.md`.

The capacity pre-check exists specifically to narrow a real bug it was built around: the section-save flow renames a changed section to a temp `__tmp_<id>` placeholder before writing its real name+capacity in a second statement (see the section below), and that second statement failing after the first already committed would otherwise leave the section stuck on its temp name. The pre-check runs immediately before that rename-then-update dance (not at the top of the save, with several unrelated writes in between) and the rename step itself is skipped for capacity-only changes, but a small window remains between "checked safe" and "written" — a reservation landing in that gap can still trigger the same corruption, just far less often. Not a closed hole, a narrowed one; see `ISSUES.md`'s Decided note for the full reasoning and what a complete fix would look like.

---

## Menu

### `menu_categories`
`restaurant_id`-scoped, `name` (unique per restaurant) + `display_order` (owner-controlled via reorder buttons, no gap-free requirement).

### `menu_items`
| Column | Type | Notes |
|---|---|---|
| `restaurant_id` | uuid FK, cascade | |
| `category_id` | uuid FK → `menu_categories`, **set null** on delete | nullable — items can be uncategorized |
| `name`, `description` | text | |
| `price` | numeric(10,2), `>= 0` | no currency column — RSD assumed, app is Serbian-only elsewhere too |
| `image_url` | text, nullable | **no owner upload UI yet** — when null, `MenuItemImage` renders an inline SVG placeholder client-side instead of a stored default image |
| `is_available` | boolean, default true | |
| `display_order` | integer | |

### `menu_item_options`
A modifier group on one item (e.g. required single-select "Veličina"). `menu_item_id` FK, cascade. `is_required`, `allow_multiple` booleans. No `restaurant_id` column — ownership is checked by joining up through `menu_item_id`.

### `menu_item_option_choices`
The actual pickable values within a group (e.g. "Mala"/"Srednja"/"Velika"). `option_id` FK, cascade. `price_delta numeric(10,2)` applied on top of the item's base price.

**RLS for all four menu tables:** select open to any authenticated user; insert/update/delete restricted to the owner, verified by joining all the way up to `restaurants.owner_id`. `menu_items.category_id` and `menu_item_options`/`_choices`'s parent references get the same same-restaurant cross-check as `tables.layout_id`/`section_id` above.

---

## Orders (Cart)

The customer-ordering flow: browse menu → build cart → confirm cart + reservation together in one atomic step. See `ISSUES.md`'s "Customer ordering" Decided notes for the full design story.

### `orders`
| Column | Type | Notes |
|---|---|---|
| `restaurant_id`, `customer_id` | uuid FK, cascade | |
| `status` | text | `draft` \| `confirmed` |
| `reservation_id` | uuid FK → `reservations`, **no cascade** (default RESTRICT) | an order is a financial record — shouldn't vanish if reservation deletion is ever added |
| `confirmed_at` | timestamptz, nullable | |
| — | **partial unique index** | `(customer_id) where status = 'draft'` — a customer has at most **one** draft order at a time, restaurant-agnostic; starting a cart at a different restaurant replaces the old one |

**RLS:** select-only grant, same reasoning as `reservations` — every write goes through `start_cart()`/`add_order_item()` (cart building) or `create_reservation()` (finalizing). Drafts are private to the customer; owners/staff only see `confirmed` orders (a draft is a private basket, like an un-placed Wolt cart).

### `order_items`
| Column | Type | Notes |
|---|---|---|
| `order_id` | uuid FK → `orders`, cascade | |
| `menu_item_id` | uuid FK → `menu_items`, **set null** on delete | historic lines survive a menu item being deleted later |
| `item_name`, `unit_price` | text / numeric | **snapshots** taken at add-to-cart time, computed server-side by `add_order_item()` — never client-supplied |
| `quantity` | integer, `> 0` | the *only* thing a customer can change after adding a line |

**RLS:** select scoped like `orders`. Update/delete allowed only while the parent order is still `draft`. The update policy only allows quantity to change in practice — see the Functions callout below for how that got enforced (it took three attempts).

### `order_item_choices`
Selected modifier choices for one `order_items` line — `option_name`/`choice_name`/`price_delta` snapshots, purely descriptive (the parent line's `unit_price` already has every delta baked in). Select-only grant; rows are only ever written by `add_order_item()`.

---

## Functions (`security definer` RPCs)

Postgres functions the frontend calls directly via `supabase.rpc(name, args)`, rather than composing raw table queries. **Written in `supabase/migrations/` alongside the tables** — a function is schema too (`create function` is DDL, same category as `create table`), so it has to be replayable in the same ordered, versioned system as everything else. See the design-chat discussion for the longer version, but short version: once business logic moves into the database (specifically so it can run inside a transaction with row locks and see past RLS on purpose), it inherits the database's own version-control mechanism, not the app's file layout.

`security definer` means the function runs with its *creator's* privileges instead of the caller's — so it can deliberately see through RLS, but only to do the one narrow thing it's written for. Every one of these also sets `search_path = ''` and schema-qualifies every reference (`public.foo`), which is this project's standing convention to avoid search-path hijacking.

| Function | Defined in | Called from | Does |
|---|---|---|---|
| `handle_new_user()` | [create_profiles.sql](supabase/migrations/20260826204500_create_profiles.sql) | trigger (`after insert on auth.users`), never called directly | Copies signup metadata into a new `profiles` row in the same transaction as the `auth.users` insert |
| `is_restaurant_owner_of_employee(employee uuid)` | [fix_restaurant_staff_profile_policy_recursion.sql](supabase/migrations/20260831151500_fix_restaurant_staff_profile_policy_recursion.sql) | used inside a `profiles` RLS policy, not called directly | Lets an owner's `profiles` select-policy check `restaurant_staff` without RLS-recursing back into itself (Postgres error `42P17`) |
| `create_reservation(...)` | [create_reservations.sql](supabase/migrations/20260908130000_create_reservations.sql), extended in [create_reservation_finalize_order.sql](supabase/migrations/20260911111500_create_reservation_finalize_order.sql), overlap checks widened in [reservation_status_lifecycle.sql](supabase/migrations/20260917140000_reservation_status_lifecycle.sql) | [reservation-form.tsx:205](src/components/reservation-form.tsx) | Validates hours/duration/capacity, auto-assigns tables or sections, inserts the reservation, and — if a cart order id is passed — finalizes that order in the same transaction. Locks the restaurant row first so concurrent bookings can't race. |
| `is_active_reservation_status(p_status)` | [reservation_status_lifecycle.sql](supabase/migrations/20260917140000_reservation_status_lifecycle.sql) | every capacity/availability function and guard trigger below, not called directly | `true` for `confirmed`/`preparing_order`/`order_prepared`/`ongoing` — the single definition of "this reservation is still occupying a table/section," used everywhere that used to hardcode `status = 'confirmed'` |
| `finish_expired_reservations()` | [auto_finish_reservations.sql](supabase/migrations/20260918120000_auto_finish_reservations.sql) | `pg_cron` job `finish-expired-reservations`, every minute — not callable by any app role (execute revoked from `public`/`anon`/`authenticated`/`service_role`) | Sweeps reservations whose `ends_at` has passed: `ongoing` → `completed`, `confirmed`/`preparing_order`/`order_prepared` → `no_show`. Leaves `cancelled` and already-terminal ones alone. Deliberately not routed through `update_reservation_status()` (staff-only, single-transition validation) |
| `update_reservation_status(p_reservation_id, p_new_status)` | [reservation_status_lifecycle.sql](supabase/migrations/20260917140000_reservation_status_lifecycle.sql) | [employee-restaurant-reservations-list.tsx](src/components/employee-restaurant-reservations-list.tsx) | Validates the caller is accepted staff at the reservation's restaurant, enforces the status-lifecycle transition graph (see the callout above), and — when completing or no-showing a reservation before its booked `ends_at` — shrinks `ends_at` on both `reservations` and its `reservation_tables` rows down to (approximately) `now()`, freeing the exclusion constraint for the rest of the original slot; when starting a reservation (`ongoing`) before its booked `starts_at`, moves `starts_at` up to `now()` the same way (only within 60 minutes of the booked start; rejected if that collides with another reservation on one of its tables; rejected outright once `ends_at` has passed) — latest version in [ongoing_moves_start_to_now.sql](supabase/migrations/20260918100000_ongoing_moves_start_to_now.sql) |
| `get_occupied_table_ids(...)` | [get_occupied_table_ids.sql](supabase/migrations/20260908150000_get_occupied_table_ids.sql), widened in [reservation_status_lifecycle.sql](supabase/migrations/20260917140000_reservation_status_lifecycle.sql) | [reservation-form.tsx:101](src/components/reservation-form.tsx) | Live preview of which tables are booked in a candidate time range, without exposing whose reservation it is |
| `get_section_remaining_capacity(...)` | [get_section_remaining_capacity.sql](supabase/migrations/20260908160000_get_section_remaining_capacity.sql), widened in [reservation_status_lifecycle.sql](supabase/migrations/20260917140000_reservation_status_lifecycle.sql) | [reservation-form.tsx:131](src/components/reservation-form.tsx) | Same idea, per-section remaining capacity when there's no active layout |
| `start_cart(p_restaurant_id)` | [start_cart.sql](supabase/migrations/20260911104500_start_cart.sql) | [menu-browser.tsx:96](src/components/menu-browser.tsx) | Returns the customer's existing draft for this restaurant, or replaces a draft at a different restaurant with a fresh one |
| `add_order_item(...)` | [add_order_item.sql](supabase/migrations/20260911110000_add_order_item.sql), revised in [add_order_item_dedupe.sql](supabase/migrations/20260911120000_add_order_item_dedupe.sql) | [menu-browser.tsx:105](src/components/menu-browser.tsx) | Validates the item/choices, computes `unit_price` server-side, and either inserts a new cart line or bumps an existing identical line's quantity |
| `protect_order_item_immutable_columns()` | [order_items_immutability_trigger.sql](supabase/migrations/20260911122000_order_items_immutability_trigger.sql), fixed in [order_items_trigger_allow_menu_item_set_null.sql](supabase/migrations/20260911123000_order_items_trigger_allow_menu_item_set_null.sql) | trigger (`before update on order_items`), never called directly | Rejects any update to `order_id`/`item_name`/`unit_price` — quantity is the only editable column |
| `restaurant_peak_reserved_capacity(p_restaurant_id)` / `section_peak_reserved_capacity(p_section_id)` | [protect_capacity_from_active_reservations.sql](supabase/migrations/20260913140000_protect_capacity_from_active_reservations.sql), widened in [reservation_status_lifecycle.sql](supabase/migrations/20260917140000_reservation_status_lifecycle.sql) | the two capacity-decrease triggers below, not called directly | Sweep-line over active, not-yet-ended reservations' `[starts_at, ends_at)` ranges (and party sizes) to compute the highest concurrently-committed total at any single instant — not just each reservation checked in isolation |
| `prevent_table_delete_with_active_reservation()` / `prevent_section_delete_with_active_reservation()` | same file, message detail extended in [detailed_active_reservation_errors.sql](supabase/migrations/20260917100000_detailed_active_reservation_errors.sql), widened in [reservation_status_lifecycle.sql](supabase/migrations/20260917140000_reservation_status_lifecycle.sql) | triggers (`before delete on tables` / `before delete on sections`), never called directly | Reject the delete outright (including when triggered by a `layouts` cascade) if an active, not-yet-ended reservation still references that table/section via `reservation_tables`/`reservation_sections` — the raised message names the specific table/section and its earliest active reservation's date/time and party size |
| `prevent_restaurant_capacity_below_reserved()` / `prevent_section_capacity_below_reserved()` | [protect_capacity_from_active_reservations.sql](supabase/migrations/20260913140000_protect_capacity_from_active_reservations.sql) | triggers (`before update on restaurants` / `before update on sections`, only when `capacity` actually changes), never called directly | Reject a `capacity` update below the peak figure the corresponding function above computes; a `restaurants.capacity` set to `null` (unlimited) always passes |
| `tables_with_active_reservations(p_table_ids)` | [detailed_active_reservation_errors.sql](supabase/migrations/20260917100000_detailed_active_reservation_errors.sql), extended with a `layout_name` column in [tables_with_active_reservations_layout_name.sql](supabase/migrations/20260917110000_tables_with_active_reservations_layout_name.sql), scoped to the caller's own restaurants in [scope_capacity_functions_to_owner.sql](supabase/migrations/20260917130000_scope_capacity_functions_to_owner.sql), widened in [reservation_status_lifecycle.sql](supabase/migrations/20260917140000_reservation_status_lifecycle.sql) | [edit-restaurant-form.tsx](src/components/edit-restaurant-form.tsx), in `handleSubmit`'s pre-check before any delete is attempted | Given a set of table ids, report each one owned by the caller that still has an active reservation (name, its layout's name, earliest reservation's date/time + party size), so a single save can list every blocker at once (grouped by layout once more than one is involved) instead of discovering them one `.delete()` at a time |
| `sections_with_active_reservations(p_section_ids)` | [detailed_active_reservation_errors.sql](supabase/migrations/20260917100000_detailed_active_reservation_errors.sql), scoped to the caller's own restaurants in [scope_capacity_functions_to_owner.sql](supabase/migrations/20260917130000_scope_capacity_functions_to_owner.sql), widened in [reservation_status_lifecycle.sql](supabase/migrations/20260917140000_reservation_status_lifecycle.sql) | [edit-restaurant-form.tsx](src/components/edit-restaurant-form.tsx), same pre-check | Same idea as `tables_with_active_reservations`, for sections (no layout grouping needed - section names are unique per restaurant) |
| `sections_peak_reserved_capacity(p_section_ids)` | [sections_peak_reserved_capacity_batch.sql](supabase/migrations/20260917120000_sections_peak_reserved_capacity_batch.sql), scoped to the caller's own restaurants in [scope_capacity_functions_to_owner.sql](supabase/migrations/20260917130000_scope_capacity_functions_to_owner.sql) | [edit-restaurant-form.tsx](src/components/edit-restaurant-form.tsx), pre-checked immediately before the section rename-then-update dance (see the deletion/capacity guards callout above for why, and for the residual race this doesn't fully close) | Batched wrapper around `section_peak_reserved_capacity()` — given a set of section ids, reports each one owned by the caller with its name and current peak reserved capacity, so a save touching several sections' capacity at once can name every one that would fail instead of only the first |

**Worth knowing:** locking down "only quantity may change" on `order_items` took three migrations to get right — a column-level `grant update (quantity)` didn't reliably block other columns, a `with check` self-referencing subquery also rejected legitimate quantity updates, and the working fix ended up being a plain `before update` trigger comparing `OLD`/`NEW` directly. A reasonable thing to mention if a mentor asks "did everything work first try" — it didn't, and the fix is visible in the migration history.

---

## RLS pattern used throughout

Almost every table follows one of two shapes:

1. **Public-read, owner-write:** `select` open to any authenticated user (needed for browsing), `insert`/`update`/`delete` restricted to the resource's owner — used by `restaurants`, `restaurant_hours`, `sections`, `layouts`, `tables`, and all four menu tables.
2. **Function-only-write:** no insert/update/delete grant to `authenticated` at all — every write goes through a `security definer` function. Used by `reservations`, `reservation_tables`, `reservation_sections`, `orders`, and `order_item_choices`, since these all need validation (hours, capacity, pricing) that a plain RLS policy can't express.

`tables`, `sections`, and `restaurants` are pattern 1 (owner-write directly, no RPC needed for a plain delete/rename) but additionally carry a `before delete`/`before update` trigger layered on top (see the deletion/capacity guards callout above and the Functions table below) — RLS decides *who* can write, the trigger decides whether *this particular* write is safe given other tables' data, the same division of labor `create_reservation()` already uses.

One real bug is worth knowing about since it shows *why* the cross-table checks matter: the original `tables` insert/update policies checked that `restaurant_id` belonged to the caller, but not that `layout_id`/`section_id` belonged to *that same* restaurant — since those ids aren't secret (same public-read policy as everything else), an owner could otherwise attach their own restaurant's table to a different restaurant's layout or section via a direct API call. Fixed in [tables_rls_verify_layout_and_section_restaurant.sql](supabase/migrations/20260908110000_tables_rls_verify_layout_and_section_restaurant.sql) — the same pattern was then applied proactively to `menu_items.category_id` and the menu option tables from the start.
