// Shared across every /employee page that renders AppHeader's dropdown menu,
// so the link's label/href can't drift between pages. Left off the apply
// page itself since linking to the current page is pointless.
export const EMPLOYEE_MENU_ITEMS = [{ label: "Prijavi se za posao", href: "/employee/apply" }];
