# CLAUDE.md — employee app

> Status: not yet implemented. Stack decided (see root [CLAUDE.md](../CLAUDE.md)) but no code written yet.

Employee-facing application: browse/search restaurants, request to join a restaurant as staff, view and update the status of customers' reservations.

**Planned stack:** Next.js, calling Supabase directly (data/auth/realtime). Shares UI components and types with the other apps via `shared/`.

Once implemented, this file should document: this app's directory structure and its build/test/lint commands.
